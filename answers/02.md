# Task 02 Answer

1. `./tools/recruit-info` 直接告诉了 Shell 程序所在的相对路径，所以 Shell 可以直接找到并执行它；而只输入 `recruit-info` 时，Shell 会从 `PATH` 中记录的目录里寻找对应的可执行程序。原本 `PATH` 中没有 `tools` 目录，所以会提示找不到命令。

2. 输入 `ls` 时，Shell 会先解析这个命令。在我当前的环境中，`ls` 首先是 `ls --color=auto` 的别名，然后 Shell 会按照 `PATH` 中的目录顺序查找对应的可执行文件。

